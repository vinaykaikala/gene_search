language: python

python:
  - 3.6

dist: trusty

notifications:
  email: false

install:
  - python setup.py install
  - pip install -r requirements.txt
  - pip install .

script:
  - python -m pytest
